import { defineComponent } from '@zeix/le-truc'

/**
 * The heart of the Atelier: shows and edits the component selected in the
 * component tree. Placeholder for now — module-listnav passes the selected
 * component's partial URL into the nested module-lazyload, which displays it.
 * Direct manipulation editing is to be implemented in later steps.
 */
export default defineComponent('module-component-editor', () => {
	// Deliberately empty until the editor logic is implemented.
})
