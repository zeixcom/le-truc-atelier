import { defineComponent } from '@zeix/le-truc'

/**
 * Lists the properties of the component (or selected sub-element) shown in
 * the editor area: reactive properties and addressed descendants. Placeholder
 * for now — the panel content is static markup mirroring demo-greeter's
 * property model. Introspection is to be implemented in later steps.
 */
export default defineComponent('module-properties-editor', () => {
	// Deliberately empty until the property introspection is implemented.
})
